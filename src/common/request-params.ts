/**
 * Constants to be used across different files of this worker
 *
 */
export class RequestParams {

    /**
     * Param `tags` is used within versioning component to get origin resource version for each tag.
     *
     * @type {string}
     */
    static readonly TAGS = 'tags';
}